# plan-mode-autoallow

Claude Code가 plan mode에 있는 동안 권한 프롬프트를 줄이는 `PreToolUse` 훅. 읽기 전용 작업은 통과시키고, 기계를 바꾸는 것은 평소의 프롬프트로 보낸다.

## Language

### 판정

**판정**:
훅이 한 도구 호출에 대해 내리는 결론. 값은 셋뿐이다 — 자동 허용, 넘김, 그리고 계층별 거부.
_Avoid_: 검사, 승인, 결정

**자동 허용**:
훅이 `permissionDecision: "allow"`를 발화해 프롬프트 없이 도구를 실행시키는 것.
_Avoid_: 통과, 승인, 화이트리스트

**넘김**:
훅이 아무 결정도 내지 않아 판정이 Claude Code의 평소 권한 흐름으로 내려가는 것. 대개 프롬프트가 뜨지만 `permissions.allow` 규칙이 덮을 수도 있으므로, 넘김은 프롬프트를 **뜻하지 않는다**.
_Avoid_: 차단, 거절, 프롬프트

**거부**:
어느 한 판정 계층이 "이건 내가 허용하지 않는다"고 결론 내린 것. 훅 전체의 결과가 아니라 **계층의 결론**이다 — 정적 파서가 거부해도 LLM 분류기가 허용하면 최종 결과는 자동 허용이다. 훅이 Claude Code에 `deny`를 발화하는 일은 없다.
_Avoid_: 차단, 금지

### 판정 계층

**정적 파서**:
명령줄을 토큰화해 모든 명령이 읽기 전용 목록에 있는지 확인하는 계층. 네트워크도 토큰도 쓰지 않는다. 표현할 수 있는 것만 허용하므로 셸 원라이너 앞에서 좁다.
_Avoid_: 정규식, 룰 엔진, 휴리스틱

**LLM 분류기**:
정적 파서가 거부한 명령줄만 받아 읽기 전용 여부를 판단하는 계층. 입력은 명령 문자열 하나뿐이다.
_Avoid_: AI 검사, 모델, auto 모드

**읽기 전용**:
실행해도 기계의 상태를 바꾸지 않는 것. 파일·프로세스·네트워크 자원·원격 저장소 어느 것도 쓰거나 지우거나 만들지 않는다. 두 계층이 답하는 질문은 이것 하나로 같다.
_Avoid_: 안전, 무해, 비파괴

### 적립

**승격 후보**:
LLM 분류기는 읽기 전용이라 했지만 정적 파서는 표현하지 못한 명령. 정적 파서를 넓힐 근거가 되므로 따로 쌓는다.
_Avoid_: 캐시 항목, 허용 로그

**미해결 명령**:
두 계층 모두 거부해서 넘김으로 끝난 명령. 쓰기 명령이거나, 아직 아무도 판단할 수 없는 것이다.
_Avoid_: 거부 로그, 실패, 차단된 명령
